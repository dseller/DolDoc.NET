I64 global_a;
I64 a;

struct MyStruct {
    I64 A;
    I64 B;
}

I64 square(I64 x) { return x*x; }
I64 get_42() { return 42 + global_a; }

// Comment

I64 scope_test() {
    a = 42;
    {
        I64 a = 16;
        LogInfo(Str(a));
    }
    LogInfo(Str(a));
    
    return a;
}

U0 my_function()
{
    MyStruct s = new MyStruct();
    s.A = 100;
    s.B = s.A + 20;
    LogInfo(Str(s.B));
    LogInfo(ToBinary(s.B));
}
